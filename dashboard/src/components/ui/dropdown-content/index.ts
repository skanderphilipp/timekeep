/**
 * DropdownContent — backward-compatibility stub.
 *
 * @deprecated The content wrapper is now built into the `Dropdown` component.
 *   No wrapper needed — just put <MenuItem> children directly inside <Dropdown>.
 *
 * This export exists only to prevent import breakage.
 */
export { DropdownContent } from "./dropdown-content";
