/**
 * Shared module — cross-cutting code used by multiple domain modules.
 *
 * This is NOT a domain module. It contains:
 * - Cross-cutting composite components (components/)
 * - Shared hooks, utilities, and types (future)
 *
 * Import like: import { DataBoundary, Timeline } from "@/modules/shared/components";
 */

export * from "./components";
